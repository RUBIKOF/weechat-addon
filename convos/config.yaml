name: Convos IRC Web Bouncer
version: "1.2.1" # 👈 ¡Incrementamos la versión!
slug: convos_irc_web
description: "A self-hosted, always-on IRC web client (bouncer) with modern features."
arch:
  - aarch64
  - amd64
  - armv7
url: "https://github.com/RUBIKOF/weechat-addon"
webui: http://[HOST]:[PORT:8080]
panel_icon: mdi:chat-processing
ports:
  8080/tcp: 8080 # Puerto de la interfaz web
schema:
  log_level: list(trace|debug|info|warning|error|fatal)?
  username: str
  password: str
build:
  args:
    # Mantenemos esta sección, pero el Dockerfile ahora usa una imagen directa.
    BUILD_FROM: "ghcr.io/home-assistant/{ARCH}-base-debian:latest"
    
